Final pool of implemented tasks from BenchTasksCollv3 (checked against tasks/examples requirements).

This branch aggregates all tasks whose implementation satisfies the task requirements
(e.g. docs/task.md and docs/agent_system_prompt.md exist, are non-empty and all-English,
with optional files only needing to exist).
